{
  "name": "maw-crew-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "maw plugin: crew — spawn/verify/teardown codex crews",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/bun": "latest",
    "@types/node": "latest"
  }
}
